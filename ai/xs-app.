{
  "welcomeFile": "/index.html",
  "authenticationMethod": "xsuaa",
  "routes": [
    {
      "source": "^/ai/(.*)",
      "destination": "ai-api",
      "csrfProtection": true,
      "authenticationType": "xsuaa",
      "scope": ["$XSAPPNAME.User"]
    }
  ]
}