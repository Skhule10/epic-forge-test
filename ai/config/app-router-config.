{
    "authenticationMethod": "route",
    "routes": [
        {
            "source": "^/fastapi/(.*)$",
            "destination": "fastapi-app",
            "csrfProtection": true
        }
    ]
}